{% for flashMessage in app.session.flashbag.get('info') %}

    <div class="alert alert-primary" role="alert">
        <h4 class="alert-heading">Hey, </h4>
        <p>{{ flashMessage }}</p>
        <hr>
        <p class="mb-0">Thank your...</p>
    </div>

{% endfor %}